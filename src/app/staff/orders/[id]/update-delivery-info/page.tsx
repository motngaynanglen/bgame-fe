export default function UpdateDeliveryInfoPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1>Cập nhật thông tin giao hàng cho đơn hàng {params.id}</h1>
      {/* Thêm form hoặc giao diện cập nhật thông tin giao hàng ở đây */}
    </div>
  );
}
